# Production requirements — slimmed down for Render free tier (512MB RAM)
# ใช้ pre-computed embeddings (ไม่ต้องลง sentence-transformers)

--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.3.0+cpu
torch-geometric==2.5.0
numpy==1.26.4
pandas==2.2.0
scipy==1.12.0
fastapi==0.110.0
uvicorn[standard]==0.27.0
pydantic==2.6.0
matplotlib==3.8.0
