# Production requirements — ใช้ pre-computed embeddings (ไม่มี sentence-transformers)
# Flexible versions ให้รองรับหลาย Python version

--extra-index-url https://download.pytorch.org/whl/cpu

torch>=2.3,<2.12
torch-geometric>=2.4
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.10
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
matplotlib>=3.7
